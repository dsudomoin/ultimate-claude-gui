# План: обновление test.md

## Context

Файл `test.md` служит тестовой площадкой для ручной проверки рендеринга markdown в плагине Claude Code GUI. Текущая версия минимальна. Цель — обогатить его разнообразными markdown-конструкциями, чтобы визуально проверить больше сценариев рендеринга за один взгляд.

## Целевой файл

- `test.md` (корень проекта)

## Предлагаемое содержимое

```markdown
# Claude Code GUI — тестовая площадка

Этот файл используется для ручного тестирования рендеринга markdown в плагине.

---

## Форматирование текста

Обычный текст, **жирный**, *курсив*, ~~зачёркнутый~~, `инлайн-код`.

> Цитата: «Любой достаточно продвинутый инструмент неотличим от магии.»

---

## Блоки кода

### Kotlin

```kotlin
data class Message(
    val role: String,
    val content: String,
    val timestamp: Long = System.currentTimeMillis()
)

fun List<Message>.lastUserMessage(): Message? =
    lastOrNull { it.role == "user" }
```

### TypeScript (bridge)

```typescript
async function sendMessage(text: string): Promise<void> {
    const response = await sdk.messages.create({
        model: "claude-opus-4-6",
        max_tokens: 1024,
        messages: [{ role: "user", content: text }],
    });
    console.log(response.content[0]);
}
```

### JSON

```json
{
  "type": "tool_use",
  "id": "toolu_01abc",
  "name": "read_file",
  "input": {
    "path": "/src/main.kt"
  }
}
```

---

## Списки

### Маркированный

- Рендеринг `MarkdownRenderer`
- Блоки `ToolUseBlock`
- Панель `MessageListPanel`
  - Скроллинг
  - Подгрузка истории

### Нумерованный

1. Открыть чат
2. Отправить сообщение
3. Убедиться, что ответ отображается корректно
4. Проверить блок кода на выделение синтаксиса

---

## Таблица

| Компонент        | Статус        | Приоритет |
|------------------|---------------|-----------|
| MessageListPanel | в разработке  | высокий   |
| ToolUseBlock     | в разработке  | высокий   |
| ChatInput        | готово        | —         |
| MarkdownRenderer | в разработке  | средний   |
| PlanActionPanel  | новый         | средний   |

---

## Ссылки (не кликабельны в превью)

- Документация: https://docs.anthropic.com
- Репозиторий: https://github.com/anthropics/claude-code

---

## Заметки

- Файл можно редактировать свободно — изменения не влияют на сборку.
- Добавляйте сюда новые конструкции по мере появления новых компонентов рендеринга.
```

## Шаги реализации

1. Перезаписать `test.md` предложенным содержимым (через инструмент Write).

## Проверка

Открыть файл в плагине через чат и убедиться, что:
- заголовки разных уровней отображаются с правильной иерархией
- все три блока кода подсвечены синтаксисом
- таблица выровнена
- маркированный и нумерованный списки выглядят корректно
- цитата (`>`) отрендерена
- инлайн-форматирование (**жирный**, *курсив*, `код`, ~~зачёркнутый~~) работает
