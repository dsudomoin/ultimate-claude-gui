# План: Обновление test.md

## Context
Файл test.md используется как демонстрационный/тестовый файл в проекте Claude Code GUI (IntelliJ-плагин). Нужно заменить текущий шуточный placeholder на что-то более осмысленное и связанное с проектом.

## Предлагаемый текст

```markdown
# Claude Code GUI — тестовая площадка

Этот файл используется для ручного тестирования плагина в IntelliJ IDEA.

## Что здесь тестируем

- Отображение markdown в чате
- Рендеринг блоков кода
- Работу `ToolUseBlock` и `MessageListPanel`
- Скроллинг и пагинацию сообщений

## Пример блока кода

```kotlin
fun greet(name: String): String {
    return "Hello, $name!"
}
```

## Пример таблицы

| Компонент        | Статус       |
|------------------|--------------|
| MessageListPanel | в разработке |
| ToolUseBlock     | в разработке |
| ChatInput        | готово       |

## Заметки

- Файл можно редактировать свободно
- Изменения не влияют на сборку проекта
```

## Файл для изменения
- `test.md` (корень проекта)

## Проверка
Открыть файл в редакторе и убедиться, что содержимое отражает реальный контекст проекта.
